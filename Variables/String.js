// Properties and Methods

const s = 'Hello World';

console.log(s.length); // Propriedade não possui ( ). Se tiver ( ) então é um Método.
console.log(s.toUpperCase()); // Métodos são basicamentes Funções associadas a objetos.
console.log(s.substring(0, 5));
console.log(s.split(''));

const w = 'technology, computers, it, code';

console.log(w.split(', '));